export interface EmployeeFormValue {
    first_name: string,
    last_name: string,
    age: number,
    job_title: string;
  }
  
  export enum LOGIN_FORM_NAMES {
    FIRST_NAME = 'first_name',
    LAST_NAME = 'last_name',
    AGE = "age",
    JOB_TITLE = "job_title"
  }
  
